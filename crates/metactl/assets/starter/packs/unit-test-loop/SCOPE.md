# Choosing what to run

Start with the smallest command that exercises your edit (single test name, file-scoped run, or package filter). Only widen scope after the first signal. Record the exact command in notes or the PR so others can reproduce.
