# Tests and verification

Run the narrowest tests that cover touched code paths. After structural refactors, expand to module- or service-level tests once the new seams stabilize. Do not silence failures—fix or bisect until the suite is green or an explicit owner approves a scoped waiver.
